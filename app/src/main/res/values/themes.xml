<resources>

    <style name="Theme.FormatFrute" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/cream</item>
    </style>

    <!-- Splash de sistema: aparece antes do Compose desenhar o primeiro frame. -->
    <style name="Theme.FormatFrute.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.FormatFrute</item>
    </style>

</resources>
